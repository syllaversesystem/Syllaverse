/* Shared CRUD helpers for Master Data (stubs) */
export function showToast(message, type = 'info') {
  // Implement toast using Bootstrap or custom UI if needed
  // eslint-disable-next-line no-console
  console.log(`[${type}]`, message);
}
